% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Source_MT5.R
\name{MT5.StreamTicksLive}
\alias{MT5.StreamTicksLive}
\title{Stream live raw ticks without repeating the same tick}
\usage{
MT5.StreamTicksLive(
  sSymbols,
  sType = "all",
  iRows = 2000,
  fSleep = 0.25,
  iSeconds = Inf,
  FUN = NULL,
  bPrint = TRUE
)
}
\arguments{
\item{sSymbols}{character(); vector of target symbols.}

\item{sType}{character; which tick stream should be requested. Supported values are \code{"all"}, \code{"info"} and \code{"trade"}. (default: \code{"all"})}

\item{iRows}{int; maximum number of ticks requested per polling cycle and per symbol. (default: \code{2000})}

\item{fSleep}{numeric; seconds between polling cycles. (default: \code{0.25})}

\item{iSeconds}{numeric; total running time in seconds. Use \code{Inf} to keep running until interrupted. (default: \code{Inf})}

\item{FUN}{function or \code{NULL}; optional callback called with each batch of new ticks. (default: \code{NULL})}

\item{bPrint}{bool; print each new batch to the console. (default: \code{TRUE})}
}
\value{
Returns \emph{Data.frame} \eqn{[nx9]} with columns \code{sSymbol} plus the columns of \code{\link[mt5R:MT5.GetTicks]{MT5.GetTicks()}}.
}
\description{
Polls MT5 for new ticks of one or more symbols and only prints or forwards ticks which have not been seen yet.
}
\details{
The function tracks \code{TimeMsc} and a tick fingerprint to avoid repeating the same row across polling cycles.
This makes it suitable as a backend for live candlestick or quote charts that need every new tick only once.
}
\author{
Guilherme Kinzel, \email{guikinzel@gmail.com}
}
