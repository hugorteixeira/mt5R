% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Source_MT5.R
\name{MT5.GetTicks}
\alias{MT5.GetTicks}
\title{Load raw tick data}
\usage{
MT5.GetTicks(sSymbol, iRows = 10, sType = "all")
}
\arguments{
\item{sSymbol}{character; target symbol.}

\item{iRows}{int; how many rows. It's start from last. Use \code{Inf} to request all available ticks (will auto-increase the request size until MT5 returns fewer rows). (default: \code{10})}

\item{sType}{character; which tick stream should be requested. Supported values are \code{"all"}, \code{"info"} and \code{"trade"}. (default: \code{"all"})}
}
\value{
Returns \emph{Data.frame} \eqn{[nx8]}, with follow informations:
\itemize{
\item Datetime \code{{POSIXct}}: Datetime of tick.
\item Bid \code{{numeric}}: Bid price in the tick.
\item Ask \code{{numeric}}: Ask price in the tick.
\item Last \code{{numeric}}: Last traded price in the tick.
\item Volume \code{{numeric}}: Tick volume.
\item Flags \code{{int}}: MT5 tick flags.
\item VolumeReal \code{{numeric}}: Real volume reported by MT5.
\item TimeMsc \code{{numeric}}: Tick timestamp in milliseconds since Unix epoch.
}
}
\description{
Function to load raw MT5 ticks of target symbol.

Unlike \code{\link[mt5R:MT5.GetTimesSales]{MT5.GetTimesSales()}}, this function can return quote ticks (Bid/Ask updates), trade ticks, or all ticks available in MT5.
}
\details{
For many brokers, \code{sType = "info"} is the best choice to reproduce real-time quote movement.
\code{sType = "trade"} can be empty on symbols where the broker does not expose trade ticks.
}
\references{
\url{https://www.mql5.com/en/docs/constants/structures/mqltick}
}
\author{
Guilherme Kinzel, \email{guikinzel@gmail.com}
}
