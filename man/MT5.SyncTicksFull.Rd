% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Source_MT5.R
\name{MT5.SyncTicksFull}
\alias{MT5.SyncTicksFull}
\title{Synchronize a local tick file with MT5}
\usage{
MT5.SyncTicksFull(
  sSymbol,
  sFile,
  sType = "all",
  iChunk = 2000,
  sDir = NULL,
  bShowProgress = TRUE,
  bWriteEachBatch = TRUE
)
}
\arguments{
\item{sSymbol}{character; target symbol.}

\item{sFile}{character; csv file used as the local tick cache.}

\item{sDir}{character or \code{NULL}; optional directory used together with \code{sFile}. When supplied, the file is read/written inside this directory. (default: \code{NULL})}

\item{sType}{character; which tick stream should be requested. Supported values are \code{"all"}, \code{"info"} and \code{"trade"}. (default: \code{"all"})}

\item{iChunk}{int; chunk size used in each request. (default: \code{2000})}

\item{bShowProgress}{bool; print progress while downloading. (default: \code{TRUE})}

\item{bWriteEachBatch}{bool; if \code{TRUE}, append each new batch to disk immediately so interrupted runs can be resumed safely. (default: \code{TRUE})}
}
\value{
Returns \emph{Data.frame} \eqn{[nx8]} with the same columns of \code{\link[mt5R:MT5.GetTicks]{MT5.GetTicks()}}.
}
\description{
If the local file already exists, repairs duplicated or incomplete trailing rows and downloads only ticks that are still missing.
If the file does not exist, creates it from scratch.
}
\details{
This is a convenience wrapper around \code{\link[mt5R:MT5.DownloadTicksFull]{MT5.DownloadTicksFull()}} for the common workflow of keeping a local tick cache updated across multiple runs.

The final cache file name is normalized in the same way as \code{\link[mt5R:MT5.DownloadTicksFull]{MT5.DownloadTicksFull()}}, so the resulting file ends with \code{_<sType>.csv}.
}
\author{
Guilherme Kinzel, \email{guikinzel@gmail.com}
}
